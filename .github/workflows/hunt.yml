name: Daily DR40 Hunter
on:
  workflow_dispatch:
  schedule:
    - cron: '0 14 * * *'
permissions:
  contents: write
concurrency:
  group: daily-hunter
  cancel-in-progress: false
jobs:
  hunt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: pip install requests beautifulsoup4 lxml
      - run: python hunter.py --facility daily
      - uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "auto: daily finds"
          file_pattern: "finds/daily-*.csv"
