name: CAD Facility Hunter
on:
  workflow_dispatch:
  schedule:
    - cron: '0 15 * * *'
permissions:
  contents: write
concurrency:
  group: cad-hunter
  cancel-in-progress: false
jobs:
  hunt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: pip install requests beautifulsoup4 lxml
      - run: python hunter.py --facility cad
      - uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "auto: cad finds"
          file_pattern: "finds/cad-*.csv"
